# Ruff configuration.
#
# The formatter excludes Alembic files: existing migration files must stay
# byte-identical (project rule — never modify committed migrations), and
# autogenerated migration style is not worth enforcing. Lint (`ruff check`)
# still covers them.
[format]
exclude = ["alembic/env.py", "alembic/versions/*"]
